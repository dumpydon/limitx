# Backend URL embedded into the browser bundle.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional future LLM integration. The shipped Replay Analyst uses a deterministic fallback.
OPENAI_API_KEY=
OPENAI_MODEL=

